apiVersion: apps/v1
kind: Deployment
metadata:
  name: airflow-triggerer
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.replicaCount.triggerer }}
  selector:
    matchLabels:
      app: airflow-triggerer
  template:
    metadata:
      labels:
        app: airflow-triggerer
    spec:
      containers:
      - name: triggerer
        image: {{ .Values.image.airflow }}
        args: ["triggerer"]
        resources:
{{ toYaml .Values.resources.triggerer | indent 10 }}
        envFrom:
          - configMapRef:
              name: {{ .Values.envFrom.configMapRefName | default "airflow-common" }}
          - secretRef:
              name: {{ .Values.envFrom.secretRefName | default "airflow-secrets" }}
        volumeMounts:
          - name: dags
            mountPath: /opt/airflow/dags
      volumes:
        - name: dags
          persistentVolumeClaim:
            claimName: airflow-dags-pvc