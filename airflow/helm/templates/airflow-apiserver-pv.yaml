apiVersion: v1
kind: PersistentVolume
metadata:
  name: airflow-dags-pv
spec:
  capacity:
    storage: {{ .Values.persistence.airflowApiserver.size }}
  accessModes:
    - ReadWriteMany
  hostPath:
    path: {{ .Values.persistence.airflowApiserver.path }}
  storageClassName: local-path